<% if @question.errors.any? %>
  // Render new question form with errors
  $('#new_question_form').html("<%= j render 'surveys/question_form', question: @question, survey: @survey %>");
<% else %>
  $('.new-question-modal').modal('hide');
  $('#category-pane-<%= @category.id %>').html("<%= j render 'category_pane', category: category, survey: survey  %>");
  $('#category-pane-my').html("<%= j render 'category_pane_my', survey: survey   %>");
<% end %>
